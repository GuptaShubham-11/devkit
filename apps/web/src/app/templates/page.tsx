import { GridTemplate } from "@/components/template/grid-template";

export default function () {
  return (
    <main className="font-inter mt-20 flex min-h-screen items-center justify-center">
      <GridTemplate />
    </main>
  );
}
